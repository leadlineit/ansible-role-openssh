---
- name: Converge
  hosts: all
  tasks:
    - name: Install requirements
      apt:
        name: gnupg
        state: present
        update_cache: true
    - name: "Include leadlineit.openssh"
      include_role:
        name: "leadlineit.openssh"
